<h1>Recent Photos</h1>


<%= form_with(url: 'root', method: :get, local: true) do |f| %>
   <div class="field">
    <%= f.label :flickr_user_id %>
    <%= f.text_field :flickr_user_id, autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
